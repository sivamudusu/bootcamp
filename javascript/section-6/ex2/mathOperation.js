const add = (a, b) => a + b;

const multiply = (a, b) => a * b;

const subtract = (a, b) => a - b;

const divide = (a, b) => a / b;

const squareRoot = (n) => {
  return Math.sqrt(n);
};

module.exports = { add, multiply, subtract, divide, squareRoot };
